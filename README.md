# @permission-protocol/cli

TypeScript CLI for scanning agent projects for Permission Protocol adoption work.

## Install

```bash
npm install
```

## Usage

```bash
npx @permission-protocol/cli scan .
npx @permission-protocol/cli scan ./my-agent
pp-scan --help
pp-scan --version
```

## Development

```bash
npm run typecheck
npm run build
```
